var path = require("path");

var responseArray = [];
var callIndex = 0;

function getNext() {
  const fn = responseArray[callIndex];
  callIndex += 1;
  return fn;
}

function matchesRequest(req) {
  return [<%-fns%>].reduce((res, fn) => res && fn(req), true);
}

module.exports = {getNext, matchesRequest};
